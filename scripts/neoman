#!/usr/bin/python

import sys
from PySide.QtGui import QApplication
from neoman.view import main

if __name__ == '__main__':
    app = QApplication(sys.argv)
    window = main.MainWindow()
    window.show()

    sys.exit(app.exec_())
